server:
  port: 8080

spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: public_category
          uri: lb://main-service
          predicates:
            - Path=/categories/**

        - id: public_compilation
          uri: lb://main-service
          predicates:
            - Path=/compilations/**

        - id: public_event
          uri: lb://main-service
          predicates:
            - Path=/events/**

        - id: request_service_route
          uri: lb://request-service
          predicates:
            - Path=/users/*/requests, /users/*/requests/*, /users/*/requests/*/cancel, /users/*/events/*/requests

        - id: public_user
          uri: lb://main-service
          predicates:
            - Path=/users/**

        - id: user_service_route
          uri: lb://user-service
          predicates:
            - Path=/admin/users/**


        - id: public_admin
          uri: lb://main-service
          predicates:
            - Path=/admin/**