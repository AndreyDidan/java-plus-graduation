grpc:
  server:
    port: 0
  client:
    collector:
      address: 'discovery:///aggregator'
      enableKeepAlive: true
      keepAliveWithoutCalls: true
      negotiationType: plaintext

spring:
  application:
    name: aggregator
  kafka:
    config:
      bootstrap-servers: "localhost:9092"

      producer-client-id-config: "producer-2"
      producer-key-serializer: "org.apache.kafka.common.serialization.StringSerializer"
      producer-value-serializer: "ru.practicum.kafka.serializer.GeneralAvroSerializer"

      consumer-group-id: "aggregator-group"
      consumer-client-id-config: "consumer-1"
      consumer-key-deserializer: "org.apache.kafka.common.serialization.StringDeserializer"
      consumer-value-deserializer: "ru.practicum.kafka.deserializer.UserActionDeserializer"
      consumer-attempt-timeout: 1000
      consumer-enable-auto-commit: false

      user-action-topic: "stats.user-actions.v1"
      events-similarity-topic: "stats.events-similarity.v1"