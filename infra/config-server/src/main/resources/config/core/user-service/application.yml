server:
  port: 0
spring:
  application:
    name: user-service
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:6543/user_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver

stats:
  service:
    enabled: true
    name: stats-service