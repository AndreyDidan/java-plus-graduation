server:
  port: 0
spring:
  application:
    name: comment-service
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:6545/comment_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver

stats:
  service:
    enabled: true
    name: stats-service